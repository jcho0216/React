import styled from "styled-components";

export const TodoTitle = styled.div`
  display: flex;
  display: -webkit-flex;
  align-items: center;
  font-size: 1.8rem;
  color: var(--white);
  margin-bottom: 10px;
`;

export const TitleIcon = styled.div`
    margin-right: 8px;
`;

export const TodoTitleTitle = styled.div`
  
`;